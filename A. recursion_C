#include <stdio.h>

int sum(int n)
{
    if (n == 1)   
        return 1;
    else          
        return n + sum(n - 1);
}

int main()
{
    int n = 3;   
    printf("The sum of the first %d integers is: %d\n", n, sum(n));
    return 0;
}

// Output: The sum of the first 3 integers is: 6
