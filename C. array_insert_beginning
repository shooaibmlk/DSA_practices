#include <stdio.h>
int main() {
    int arr[10] = {10, 20, 30, 40, 50};
    int n = 5;           
    int value = 99;      
    int i;
 for (i = n; i > 0; i--) {
        arr[i] = arr[i - 1];
    }
  arr[0] = value;
n++;
for (i = 0; i < n; i++) {
       printf("%d ", arr[i]);
    }
return 0;
}
